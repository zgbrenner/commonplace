{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Commonspace's window permissions. Deliberately minimal: the frontend cannot read or write files directly — every filesystem operation goes through typed Rust commands that pass the permission engine. The dialog plugin is allowed so the user can authorize folders with a native picker, and the opener plugin so artifacts can be opened in their default application after an explicit click.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "dialog:allow-open",
    "dialog:allow-message",
    "opener:allow-open-path",
    "opener:allow-reveal-item-in-dir"
  ]
}
